spring:
  application:
    name: messaging-service
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
    consumer:
      group-id: group-id  # just a  default value; can be overridden in the code using @KafkaListener